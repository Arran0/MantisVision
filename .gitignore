# --- Node / Next.js (apps/web) ---
node_modules/
.next/
out/
.env
.env.local
.env.*.local
npm-debug.log*
.vercel
*.tsbuildinfo

# --- Python / ML (ml) ---
.venv/
venv/
__pycache__/
*.pyc
ml/checkpoints/*
!ml/checkpoints/.gitkeep
ml/logs/
ml/reports/

# Dataset images: committed directly to git for now (MVP scale). If the
# dataset grows past ~1-2 GB, switch to Git LFS instead of removing this
# comment and re-ignoring the folder.

# --- OS / editor ---
.DS_Store
.vscode/
.idea/
