# Minimal dependency set for running src/api (the inference service) in
# production. Deliberately excludes training/eval-only deps from
# requirements.txt (scikit-learn, pandas, matplotlib, seaborn, onnx,
# onnxruntime, kaggle, tqdm) to keep the container image small and the
# Render free-tier build fast.
--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.2,<3
torchvision>=0.17,<1
numpy>=1.26,<3
pillow>=10.2,<13
grad-cam>=1.5,<2
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9,<1
pydantic>=2.6,<3
