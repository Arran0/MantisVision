Add real `icon-192.png` and `icon-512.png` here before shipping — the PWA
manifest references them. Any square PNG works for local development; a
missing icon just falls back to the browser default, it won't break the app.
